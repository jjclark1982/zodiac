.lightbox-view {
    background-color: rgba(0, 0, 0, .75);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 200%;
    .container {
        overflow-y: hidden;
        height: 100%;
        padding-top: 2em;
    }
}
