<select name="{field.name}" class="input-{field.name|className}">
    <option value=""></option>
    {#field.options}
        <option value="{.}" {@if value=value is=.}selected{/if}>{.}</option>
    {/field.options}
</select>
{! todo: include @if helper !}
