{#value}
    <span name="{field.name|attr}" class="input-{field.name|className}">{value}</span>
{:else}
    <input type="text" name="{field.name|attr}" class="input-{field.name|className}" />
{/value}
