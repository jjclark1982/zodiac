{{#camelize}}{{name}}-view{{/camelize}} = require("./index")

describe("{{#camelize}}{{name}}-view{{/camelize}}", ->
    beforeEach((done)->
        @view = new {{#camelize}}{{name}}-view{{/camelize}}()
        @view.render(done)
    )

    afterEach(->
        @view?.remove()
    )

    it("should initialize and render without errors", ->
        expect(@view).to.be.ok
    )
)
